<Window x:Class="Task8.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task8.Views" 
        xmlns:viewmodels="clr-namespace:Task8.ViewModels" 
       
        mc:Ignorable="d"
         Title="Создание разреза" Height="330" Width="350"
         WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Имя разреза:"
        HorizontalAlignment="Left" Margin="0,16"
        />

        <TextBox Text="Разрез"
        Grid.Row="1" />

        <TextBlock Text="Отступы (мм):"
        Grid.Row="2"
 HorizontalAlignment="Left" Margin="0,16" FontWeight="Bold"
 />
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="По ширине:"
 HorizontalAlignment="Left" Margin="0,8"
 />
            <TextBox Text="100" Margin="0,8"
        Grid.Row="0" Grid.Column="1"  />

            <TextBlock Text="По глубине:"
            Grid.Row="1" Grid.Column="0" 
 HorizontalAlignment="Left" Margin="0,8"
 />
            <TextBox Text="100" Margin="0,8"
        Grid.Row="1" Grid.Column="1"  />

            <TextBlock Text="По высоте:"
             Grid.Row="2" Grid.Column="0" 
 HorizontalAlignment="Left" Margin="0,8"
 />
            <TextBox Text="100" Margin="0,8"
        Grid.Row="2" Grid.Column="1"  />

        </Grid>
        <Button  Content="Создать разрезы"  
        Grid.Row="4"
        Margin="0,8"
        Background="LightGray"
        Foreground ="Black" FontWeight="Bold" FontSize="16"
        Command="{Binding CaclOpening}"
        
       
       
        />

        <!--<GroupBox Header="Контроль т<!олщины"
                    Grid.Row="4"
                    Margin="16" 
                    Padding="15"
                    BorderThickness="1" 
                    BorderBrush="Gray">
            <GroupBox.Style>
                <Style TargetType="GroupBox">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding OpeningInfo.IsCorrect}" Value="True">
                            <Setter Property="Background" Value="#DFF4E0"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OpeningInfo.IsCorrect}" Value="False">
                            <Setter Property="Background" Value="#F9DDDD"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </GroupBox.Style>-->



    </Grid>
</Window>   
